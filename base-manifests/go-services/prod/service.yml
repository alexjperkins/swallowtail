apiVersion: v1
kind: Service
metadata:
  labels:
    birdperch.io/routing-name: $(SERVICE_NAME)
  name: s-
spec:
  clusterIP: None
  ports:
  - name: http
    port: 80
  selector:
    birdperch.io/routing-name: $(SERVICE_NAME)
    app: birdperch.io.$(SERVICE_NAME)
