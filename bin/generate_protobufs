#!/bin/bash

SERVICE=$1
SERVICE_NAME=$(echo "$SERVICE" | cut -c 3-100 | sed "s/\-//g")

protoc --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative $SERVICE/proto/$SERVICE_NAME.proto
